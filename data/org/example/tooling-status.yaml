# Per-tool assessments reference organization capability IDs, never MITRE or CoSAI IDs directly.
# Regenerated by scripts/specialize-capabilities.mts from the pre-migration records in archive/.
tools:
  - tool: toolClaudeCode
    available: true
    note: Approved for engineering on managed laptops; managed settings pushed by MDM.
    capabilities:
      EX-D3-MFA:
        status: enabled
        note: SSO enforced through the claude.ai organisation; forceLoginMethod claudeai and forceLoginOrgUUID pinned
          in managed-settings.json
      EX-D3-APA:
        status: enabled
        note: SSO enforced through the claude.ai organisation; forceLoginMethod claudeai and forceLoginOrgUUID pinned
          in managed-settings.json
      EX-AGENT-ACTION-POLICY-ENFORCEMENT:
        status: enabled
        note: permissions.deny rules for destructive shell commands and force-push in managed settings; a PreToolUse
          hook blocks commands that leave the repository
      EX-AML-M0037:
        status: enabled
        note: permissions.deny rules for destructive shell commands and force-push in managed settings; a PreToolUse
          hook blocks commands that leave the repository
      EX-AML-M0030:
        status: enabled
        note: permissions.deny rules for destructive shell commands and force-push in managed settings; a PreToolUse
          hook blocks commands that leave the repository
      EX-AML-M0033:
        status: enabled
        note: permissions.deny rules for destructive shell commands and force-push in managed settings; a PreToolUse
          hook blocks commands that leave the repository
      EX-AI-VENDOR-ASSESSMENT:
        status: enabled
        note: Anthropic assessed in the TPRM platform against the SOC 2 Type 2 and ISO 27001 artifacts; annual
          re-review
        evidence: TPRM-0417
      EX-SENSITIVE-DATA-REDACTION:
        status: inProgress
        note: Read() deny rules cover .env and ~/.aws; sandbox credential masking waits on the sandbox rollout
      EX-AML-M0020:
        status: inProgress
        note: Built-in detections only; WebFetch limited to allowed domains
      EX-AML-M0028:
        status: enabled
        note: permissions.deny and allowManagedPermissionRulesOnly in managed-settings.json
        evidence: CHG-1042
      EX-D3-EI:
        status: gap
        note: sandbox.enabled pending bubblewrap packaging for the Linux fleet
      EX-D3-CTS:
        status: enabled
        note: forceLoginOrgUUID; API keys blocked
      EX-D3-TB:
        status: enabled
        note: forceLoginOrgUUID; API keys blocked
      EX-AML-M0031:
        status: inProgress
        note: Managed CLAUDE.md pushed org-wide; autoMemoryDirectory not yet pinned to a per-user path
      EX-D3-CH:
        status: enabled
        note: apiKeyHelper issues short-lived gateway tokens; no long-lived key on the device
      EX-D3-CRO:
        status: enabled
        note: apiKeyHelper issues short-lived gateway tokens; no long-lived key on the device
      EX-MCP-TOOL-INTEGRITY:
        status: enabled
        note: allowedMcpServers allowlist and managed-mcp.json as the only server set — admission of servers by
          identity; tool-definition integrity is not covered and is accepted as a residual
      EX-AML-M0014:
        status: gap
        note: Vendor offers nothing; the inventory is kept by hand in the governance register
      EX-D3-FIM:
        status: enabled
        note: allowedMcpServers allowlist and managed-mcp.json as the only server set — admission of servers by
          identity; tool-definition integrity is not covered and is accepted as a residual
      EX-AML-M0029:
        status: enabled
        note: disableBypassPermissionsMode set; permissions.ask on git push and package publish
      EX-AGENT-TOOL-REGISTRY:
        status: inProgress
        note: Seats assigned per role in the admin console; agent teams disabled until the inventory is complete
      EX-SHADOW-AI-DISCOVERY:
        status: inProgress
        note: Header injected by the corporate proxy blocks personal logins (prevention); discovery of unmanaged
          installs from the EDR inventory of the claude binary is not yet reconciled against seat assignments
      EX-AML-M0024:
        status: inProgress
        note: OTel export to the SIEM from a pilot group
      EX-D3-SFA:
        status: inProgress
        note: OTel export to the SIEM from a pilot group
      EX-AGENT-KILL-SWITCH:
        status: inProgress
        note: SCIM deprovisioning removes the seat and requiredMinimumVersion forces upgrades (future access);
          stopping a session already running on a device is untested and the cessation delay has not been
          measured
      EX-D3-PT:
        status: enabled
        note: EDR agent on every managed laptop with the documented process exclusions
      EX-D3-CR:
        status: inProgress
        note: SCIM deprovisioning removes the seat and requiredMinimumVersion forces upgrades (future access);
          stopping a session already running on a device is untested and the cessation delay has not been
          measured
      EX-D3-PA:
        status: enabled
        note: EDR agent on every managed laptop with the documented process exclusions
      EX-D3-SCP:
        status: enabled
        note: Managed policy CLAUDE.md at the managed path; strictPluginOnly on
      EX-AML-M0021:
        status: enabled
        note: Managed policy CLAUDE.md at the managed path; strictPluginOnly on
      EX-AML-M0023:
        status: gap
        note: Vendor offers nothing; the inventory is kept by hand in the governance register
      EX-AML-M0013:
        status: gap
        note: Vendor offers nothing; the inventory is kept by hand in the governance register
      EX-AGENT-EGRESS-CONTROL:
        status: inProgress
        note: Gateway enforced for inference; sandbox network policy not yet rolled out
      EX-D3-NI:
        status: inProgress
        note: Gateway enforced for inference; sandbox network policy not yet rolled out
  - tool: toolCursor
    available: true
    note: Pilot with one team; Privacy Mode enforced, MCP allowlist under evaluation.
    capabilities:
      EX-D3-MFA:
        status: enabled
        note: SSO through the team dashboard; MDM AllowedTeamId pins the tenant
      EX-D3-APA:
        status: enabled
        note: SSO through the team dashboard; MDM AllowedTeamId pins the tenant
      EX-AGENT-ACTION-POLICY-ENFORCEMENT:
        status: inProgress
        note: Run Mode policy set to Auto-review for the pilot team; Allowlist mode planned
      EX-AML-M0037:
        status: inProgress
        note: Run Mode policy set to Auto-review for the pilot team; Allowlist mode planned
      EX-AML-M0030:
        status: inProgress
        note: Run Mode policy set to Auto-review for the pilot team; Allowlist mode planned
      EX-AML-M0033:
        status: inProgress
        note: Run Mode policy set to Auto-review for the pilot team; Allowlist mode planned
      EX-AI-VENDOR-ASSESSMENT:
        status: enabled
        note: Cursor assessed in the TPRM platform; ownership change flagged for re-review
        evidence: TPRM-0522
      EX-SENSITIVE-DATA-REDACTION:
        status: inProgress
        note: beforeReadFile hook redacts .env files; no beforeSubmitPrompt hook yet
      EX-AML-M0020:
        status: inProgress
        note: Auto-review classifier on; Browser Controls domain allowlist not set
      EX-AML-M0028:
        status: inProgress
        note: MCP Configuration allowlist drafted; permissions.json not yet pushed by MDM
      EX-D3-EI:
        status: gap
        note: sandbox.json not configured; developers run in the default mode
      EX-D3-CTS:
        status: gap
        note: Vendor offers nothing for the local agent — it runs with the developer's own credentials
      EX-D3-TB:
        status: gap
        note: Vendor offers nothing for the local agent — it runs with the developer's own credentials
      EX-AML-M0031:
        status: inProgress
        note: .cursor Directory Protection on and Team Rules enforced; Workspace Trust policy pending
      EX-D3-CH:
        status: inProgress
        note: Personal API keys blocked in Team Settings → Models; tool credentials in the developer's shell are not
          vaulted and .env files are readable by terminal tools
      EX-D3-CRO:
        status: inProgress
        note: Personal API keys blocked in Team Settings → Models; tool credentials in the developer's shell are not
          vaulted and .env files are readable by terminal tools
      EX-MCP-TOOL-INTEGRITY:
        status: inProgress
        note: MCP Configuration allowlist under evaluation with the pilot team
      EX-AML-M0014:
        status: gap
        note: Vendor offers nothing; kept by hand in the governance register
      EX-D3-FIM:
        status: inProgress
        note: MCP Configuration allowlist under evaluation with the pilot team
      EX-AML-M0029:
        status: enabled
        note: Auto-review Run Mode; MCP tools prompt by default
      EX-AGENT-TOOL-REGISTRY:
        status: gap
        note: Team Marketplaces not yet configured; plugins are open
      EX-SHADOW-AI-DISCOVERY:
        status: inProgress
        note: MDM AllowedTeamId blocks personal logins (prevention); endpoint inventory of Cursor installs is not yet
          reconciled against team membership
      EX-AML-M0024:
        status: inProgress
        note: Enterprise Audit Log enabled; SIEM streaming not yet connected
      EX-D3-SFA:
        status: inProgress
        note: Enterprise Audit Log enabled; SIEM streaming not yet connected
      EX-AGENT-KILL-SWITCH:
        status: inProgress
        note: SCIM deprovisioning and Model Access Control cut future access; there is no fleet-wide stop for a
          running local session, so the runbook relies on device isolation through the EDR — untested
      EX-D3-PT:
        status: enabled
        note: EDR agent with the documented process exclusions
      EX-D3-CR:
        status: inProgress
        note: SCIM deprovisioning and Model Access Control cut future access; there is no fleet-wide stop for a
          running local session, so the runbook relies on device isolation through the EDR — untested
      EX-D3-PA:
        status: enabled
        note: EDR agent with the documented process exclusions
      EX-D3-SCP:
        status: enabled
        note: Team Rules enforced from the dashboard
      EX-AML-M0021:
        status: enabled
        note: Team Rules enforced from the dashboard
      EX-AML-M0023:
        status: gap
        note: Vendor offers nothing; kept by hand in the governance register
      EX-AML-M0013:
        status: gap
        note: Vendor offers nothing; kept by hand in the governance register
      EX-AGENT-EGRESS-CONTROL:
        status: gap
        note: sandbox.json network policy not configured; the corporate proxy is the only control
      EX-D3-NI:
        status: gap
        note: sandbox.json network policy not configured; the corporate proxy is the only control
  - tool: toolClaudeManagedAgents
    available: true
    note: Design partner programme; vault scoping under review.
    capabilities:
      EX-D3-MFA:
        status: enabled
        note: Console organisation on SSO; one workspace per team
      EX-D3-APA:
        status: enabled
        note: Console organisation on SSO; one workspace per team
      EX-D3-AA:
        status: inProgress
        note: Vault display_name mapped to the owning service; no lifecycle review yet
      EX-D3-CR:
        status: inProgress
        note: Vault display_name mapped to the owning service; no lifecycle review yet Agent archive and session
          delete runbook; on-call can interrupt a session
      EX-D3-CH:
        status: inProgress
        note: Vault credentials with managed OAuth refresh for MCP; static bearer tokens still in two vaults
      EX-D3-CRO:
        status: inProgress
        note: Vault credentials with managed OAuth refresh for MCP; static bearer tokens still in two vaults
      EX-AML-M0012:
        status: gap
        note: No customer-managed keys for sessions; accepted for the design partner phase
      EX-AGENT-TOOL-REGISTRY:
        status: enabled
        note: Agents are workspace resources with metadata naming the owner
      EX-STAGED-ROLLOUT-GATE:
        status: inProgress
        note: Agent versions bumped through the deployment pipeline and pinned per environment (versioning); no canary
          exposure, and rollback to an earlier version has not been rehearsed
      EX-AML-M0008:
        status: gap
        note: No release-gate evaluation before an agent version goes live
      EX-D3-RC:
        status: inProgress
        note: Agent versions bumped through the deployment pipeline and pinned per environment (versioning); no canary
          exposure, and rollback to an earlier version has not been rehearsed
      EX-D3-RS:
        status: inProgress
        note: Agent versions bumped through the deployment pipeline and pinned per environment (versioning); no canary
          exposure, and rollback to an earlier version has not been rehearsed
      EX-D3-RD:
        status: inProgress
        note: Agent versions bumped through the deployment pipeline and pinned per environment (versioning); no canary
          exposure, and rollback to an earlier version has not been rehearsed
      EX-AML-M0028:
        status: inProgress
        note: Toolsets default to enabled false; per-tool review in progress
      EX-AML-M0029:
        status: enabled
        note: permission_policy always_ask on write tools
      EX-AML-M0020:
        status: inProgress
        note: Read-only memory stores and limited networking; no inline classifier Model-side safeguards only; no
          organisation guardrail on the session
      EX-AI-VENDOR-ASSESSMENT:
        status: enabled
        note: Covered by the Anthropic TPRM assessment
        evidence: TPRM-0417
      EX-D3-ORA:
        status: inProgress
        note: Agent and vault listings pulled into the governance register weekly by script
      EX-AGENT-TRACING:
        status: enabled
        note: Session event stream shipped to the observability platform
      EX-AML-M0024:
        status: inProgress
        note: Session events retained; export to the SIEM not yet automated
      EX-D3-SFA:
        status: inProgress
        note: Session events retained; export to the SIEM not yet automated
      EX-AML-M0031:
        status: enabled
        note: Memory stores attached read_only except the agent's own
      EX-AML-M0004:
        status: enabled
        note: Session budget set on every agent
      EX-AML-M0036:
        status: enabled
        note: Session budget set on every agent
      EX-AGENT-KILL-SWITCH:
        status: enabled
        note: Agent archive and session delete runbook; on-call can interrupt a session
      EX-D3-CI:
        status: inProgress
        note: Configuration read back through the API into the register (evidence); comparison against the policy
          baseline and drift alerting not yet built
      EX-D3-EI:
        status: enabled
        note: Cloud environments only; no self-hosted sandbox yet
      EX-AGENT-EGRESS-CONTROL:
        status: inProgress
        note: networking type limited with allowed_hosts on the production environment; two development environments
          still unrestricted, and web_fetch domain lists not yet set
      EX-D3-NI:
        status: inProgress
        note: networking type limited with allowed_hosts on the production environment; two development environments
          still unrestricted, and web_fetch domain lists not yet set
      EX-AGENT-ACTION-POLICY-ENFORCEMENT:
        status: enabled
        note: permission_policy always_ask on bash and on every MCP toolset; evaluated_permission kept from each tool
          event in the SIEM
      EX-AML-M0037:
        status: enabled
        note: permission_policy always_ask on bash and on every MCP toolset; evaluated_permission kept from each tool
          event in the SIEM
      EX-AML-M0030:
        status: enabled
        note: permission_policy always_ask on bash and on every MCP toolset; evaluated_permission kept from each tool
          event in the SIEM
      EX-AML-M0033:
        status: enabled
        note: permission_policy always_ask on bash and on every MCP toolset; evaluated_permission kept from each tool
          event in the SIEM
      EX-D3-CTS:
        status: inProgress
        note: Vault credentials substituted at egress; one shared vault per agent, so per-user vaults for tenant
          scoping are still to be built
      EX-D3-TB:
        status: inProgress
        note: Vault credentials substituted at egress; one shared vault per agent, so per-user vaults for tenant
          scoping are still to be built
      EX-MCP-TOOL-INTEGRITY:
        status: inProgress
        note: Servers declared per agent definition and gated in the deployment pipeline; default_config.enabled false
          with named tools on two of five agents
      EX-D3-FIM:
        status: inProgress
        note: Servers declared per agent definition and gated in the deployment pipeline; default_config.enabled false
          with named tools on two of five agents
  - tool: toolClaudeDesktop
    available: true
    note: Enterprise plan; connector directory restricted to approved connectors.
    capabilities:
      EX-D3-MFA:
        status: enabled
        note: Require SSO for Claude; SCIM provisioning with groups
      EX-D3-APA:
        status: inProgress
        note: Require SSO for Claude; SCIM provisioning with groups Connectors act as the user; managed authorisation
          scoping under review
      EX-D3-OTF:
        status: enabled
        note: IP allowlisting to the corporate egress ranges; tenant restrictions header on the proxy
      EX-AGENT-EGRESS-CONTROL:
        status: enabled
        note: Documented host allowlist on the proxy; egressProxyUrl pushed by MDM
      EX-SENSITIVE-DATA-REDACTION:
        status: inProgress
        note: Endpoint DLP covers the desktop app; SaaS-side inspection pending
      EX-AML-M0020:
        status: inProgress
        note: Connector tool restrictions in place; inference hooks pending
      EX-D3-WSAA:
        status: enabled
        note: Browser security extension inspects paste and upload on claude.ai
      EX-D3-NI:
        status: enabled
        note: Documented host allowlist on the proxy; egressProxyUrl pushed by MDM
      EX-D3-ORA:
        status: inProgress
        note: Compliance API pulled into the governance register; usage exports still manual
      EX-AI-VENDOR-ASSESSMENT:
        status: enabled
        note: Covered by the Anthropic TPRM assessment
        evidence: TPRM-0417
      EX-AML-M0029:
        status: enabled
        note: Connector tools prompt per call; always-allow disabled by the Owner
      EX-D3-AA:
        status: enabled
        note: Compliance and Admin API keys scoped and rotated quarterly
      EX-D3-CR:
        status: enabled
        note: Compliance and Admin API keys scoped and rotated quarterly SCIM deprovisioning; connector disable
          runbook
      EX-D3-CI:
        status: inProgress
        note: Effective settings read back through the Compliance API (evidence); comparison against the policy
          baseline and drift alerting not yet built
      EX-D3-DI:
        status: inProgress
        note: Retention set to 90 days and Compliance API delete on legal request (lifecycle); discovery and
          classification of sensitive content in chats and projects not yet connected to the DSPM product
      EX-AML-M0012:
        status: gap
        note: Customer-managed keys not requested; default encryption accepted
      EX-D3-CH:
        status: enabled
        note: Enterprise-managed connector authorisation; no per-user OAuth grants
      EX-D3-CRO:
        status: enabled
        note: Enterprise-managed connector authorisation; no per-user OAuth grants
      EX-AML-M0028:
        status: enabled
        note: Connector directory restricted; write tools off for the organisation
      EX-DATA-ACCESS-GOVERNANCE:
        status: inProgress
        note: Connectors act as the user; managed authorisation scoping under review
      EX-AGENT-TOOL-REGISTRY:
        status: enabled
        note: Connector catalogue and extension allowlist in the console
      EX-SHADOW-AI-DISCOVERY:
        status: enabled
        note: Domain capture on; personal accounts on the verified domain pulled in
      EX-AML-M0024:
        status: enabled
        note: Audit log export to the SIEM
      EX-D3-SFA:
        status: enabled
        note: Audit log export to the SIEM
      EX-AGENT-KILL-SWITCH:
        status: enabled
        note: SCIM deprovisioning; connector disable runbook
