# Illustrative organization data, not a deployment claim about a real company.
# Map each organization capability once; MITRE and CoSAI links roll up automatically.
organisation:
  name: Example Corp
  shortName: EX
capabilities:
  - id: EX-DLP
    title: Endpoint data loss prevention
    capability: tech-dlp
    surfaces:
      surfaceEndpoint:
        status: inProgress
        note: Example endpoint DLP pilot; AI prompt and upload coverage still being verified.
  - id: EX-CASB
    title: Cloud access security broker
    capability: tech-casb
    surfaces:
      surfaceSaas:
        status: inProgress
        note: Example cloud service policy rollout; inline and API coverage differ.
  - id: EX-GUARDRAILS
    title: AI gateway guardrails
    capability: tech-llm-guardrails
    surfaces:
      surfaceCloud:
        status: inProgress
        note: Example input and output policies under evaluation.
  - id: EX-SWG
    title: Secure web gateway
    capability: tech-swg
    surfaces:
      surfaceEndpoint:
        status: inProgress
        note: Example endpoint web egress controls; coverage depends on routing.
  - id: EX-FIREWALL
    title: Cloud network policy
    capability: tech-firewalls
    surfaces:
      surfaceCloud:
        status: inProgress
        note: Example workload network policy rollout.
  - id: EX-SANDBOX
    title: Agent execution sandbox
    capability: tech-sandbox
    surfaces:
      surfaceEndpoint:
        status: gap
        note: A managed execution boundary has not been deployed in this example.
  - id: EX-AUTH
    title: Enterprise identity platform
    capability: tech-authentication
    surfaces:
      surfaceEndpoint:
        status: inProgress
        note: Example SSO and MFA rollout for AI tools.
      surfaceSaas:
        status: inProgress
        note: Example federated sign-in rollout.
  - id: EX-ACCESS
    title: AI access policy service
    capability: tech-access
    surfaces:
      surfaceEndpoint:
        status: inProgress
        note: Example policy administration for permitted AI tools and actions.
      surfaceSaas:
        status: inProgress
        note: Example organization account access policies.
  - id: EX-SIEM
    title: Security event monitoring
    capability: tech-siem
    surfaces:
      surfaceEndpoint:
        status: inProgress
        note: Example AI activity feeds are being connected to the SIEM.
  - id: EX-PAM
    title: Agent credential broker
    capability: tech-pam
    surfaces: {}
  - id: EX-AGENT
    title: Agent runtime security
    capability: tech-agentic-security
    surfaces: {}
  - id: EX-AISPM
    title: AI asset posture management
    capability: tech-ai-spm
    surfaces: {}
