# Claude Desktop and claude.ai — one entity per product × reference architecture. Facts are dated
# (asOf) and cite the vendor's own documentation; re-verify before enforcing.
# Schema: data/tooling/README.md. Claude Desktop on 3P (Bedrock / Vertex / Foundry / gateway
# inference with local conversation storage) is recorded as a variant, not an entity.
vendor: anthropic
family: Claude Desktop
tools:
  - id: toolClaudeDesktop
    name: Claude (claude.ai web, mobile and Claude Desktop)
    surfaceClasses:
      - desktopApp
      - saasChat
    variants:
      - name: Claude Desktop (macOS PKG/DMG, Windows MSIX, Linux beta)
        class: desktopApp
        url: https://support.claude.com/en/articles/12611117-deploy-claude-desktop-for-macos
      - name: claude.ai web and the iOS / Android apps
        class: saasChat
        url: https://support.claude.com/en/articles/9797531-what-is-the-enterprise-plan
      - name: Claude Desktop on 3P (inference via Bedrock, Vertex, Foundry or a gateway; local history)
        class: desktopApp
        url: https://claude.com/docs/third-party/claude-desktop/overview
        note: >-
          Configured only through MDM or a bootstrap server (or the claude.ai admin console in
          beta); conversation content goes only to your endpoint; no Anthropic account identity.
    architecture: archEnterpriseAiChat
    status: ga
    asOf: "2026-09"
    summary:
      - >-
        Anthropic's chat surfaces: claude.ai in the browser, the mobile apps, and Claude Desktop
        with its Chat, Cowork and Code tabs. Chat runs on the Anthropic API under the user's
        organisation login; connectors (MCP) reach Google Drive, Microsoft 365, GitHub, Slack and
        custom remote servers through mcp-proxy.anthropic.com; desktop extensions (.mcpb) and
        local MCP servers add device-side tools. Prompts, uploads and connector results go to
        Anthropic; Team and Enterprise data is not used for training and follows the organisation's
        retention setting (indefinite by default until a custom period is set).
      - >-
        Team and Enterprise administration is in the claude.ai console: SSO (SAML/OIDC via WorkOS)
        with domain verification and "Require SSO for Claude", JIT or SCIM provisioning with
        group-to-role mapping, custom roles and groups gating Claude Code, Cowork, Chrome,
        connectors, Claude Tag and models, connector enablement and per-tool read/write
        restrictions, enterprise-managed connector authorisation through the IdP, the desktop
        extension allowlist, audit logs (180-day export) and the Compliance API (Activity Feed,
        chats, files, projects, session transcripts, effective settings), custom retention, spend
        limits, IP allowlisting, CMEK and US-only inference. Enterprise installers deploy via
        Jamf, Kandji, Intune, SCCM or GPO with device policies in com.anthropic.claudefordesktop or
        HKLM\SOFTWARE\Policies\Claude covering extensions, local MCP, Claude Code and Cowork tabs,
        auto-updates and forced org login; device policy overrides the in-console allowlist.
    facts:
      - label: Plans
        value: >-
          Free, Pro, Max (consumer terms); Team (SSO, server-managed settings, analytics);
          Enterprise (SCIM, custom roles, Compliance API, audit logs, custom retention, CMEK, IP
          allowlisting, ZDR for Claude Code, HIPAA-ready option, US-only inference).
        url: https://support.claude.com/en/articles/9797531-what-is-the-enterprise-plan
      - label: Inference & routing
        value: Anthropic API; Claude Desktop on 3P routes Chat, Cowork and Code to Bedrock, Vertex, Foundry, the Anthropic API or an LLM / Claude apps gateway with local conversation storage.
        url: https://claude.com/docs/third-party/claude-desktop/overview
      - label: Data leaving the device
        value: >-
          Prompts, attachments, artifact content and connector retrievals to Anthropic; the
          desktop app also loads application code from claude.ai and *.claudeusercontent.com CDN
          hosts; the tenant-restrictions header and IP allowlist are the network-side gates.
        url: https://code.claude.com/docs/en/desktop
      - label: Retention & training
        value: >-
          Commercial plans: no training; chat and project retention indefinite by default,
          configurable from 30 days at Organization settings > Data and privacy; audit logs 180
          days; Compliance API local/remote session transcripts 6 years or the custom period.
        url: https://support.claude.com/en/articles/10440198-configure-custom-data-retention-controls-for-enterprise-plans
      - label: Docs index
        value: support.claude.com Team and Enterprise plans collection plus platform.claude.com manage-claude (Compliance API, inference hooks)
        url: https://support.claude.com/en/articles/9797531-what-is-the-enterprise-plan
    items:
      - title: Managed deployment and device policies
        body:
          - "Device policies live in the macOS configuration-profile domain
            com.anthropic.claudefordesktop and the Windows HKLM\\SOFTWARE\\Policies\\Claude registry
            key (HKCU is ignored when HKLM exists). The keys that matter for control:
            isDesktopExtensionEnabled and isDesktopExtensionDirectoryEnabled (kill switches for
            extensions and the directory), isLocalDevMcpEnabled (local MCP server connections),
            isClaudeCodeForDesktopEnabled, secureVmFeaturesEnabled (Cowork access),
            forceLoginOrgUUID, allowedWorkspaceFolders, effortLevel, disableAutoUpdates and
            autoUpdaterEnforcementHours. Device-level policy overrides the in-app allowlist."
          - "Claude Desktop on 3P extends the same stores with inferenceProvider and credential
            keys, disabledBuiltinTools, builtinToolPolicy, disableBypassPermissionsMode,
            coworkEgressAllowedHosts, managedMcpServers, allowedPluginMarketplaces,
            userPluginMarketplacesEnabled / userPluginUploadsEnabled, sshHostAllowlist,
            egressProxyUrl and OTLP telemetry keys; a Linux /etc/claude-desktop/managed-settings.json
            uses native JSON."
        links:
          - title: Enterprise configuration for Claude Desktop
            url: https://support.claude.com/en/articles/12622667-enterprise-configuration-for-claude-desktop
          - title: Claude Desktop configuration reference (3P managed keys)
            url: https://claude.com/docs/third-party/claude-desktop/configuration
      - title: Extension allowlist and managed connector authorization
        body:
          - Team and Enterprise Owners can turn on the desktop extension allowlist (Organization
            settings > Connectors > Desktop). It ships disabled — every extension is available until
            it is enabled — and enabling it force-deletes existing non-allowlisted installs; custom
            org-scoped extensions upload in .mcpb format. Enterprise-managed connector
            authorization (GA) lets admins provision MCP connectors org-wide through the IdP (Okta
            today), assign them by role and scope the permissions Claude may request; Owners can
            also restrict each connector's tools to read-only and block verified-domain services
            from connecting to outside accounts.
        links:
          - title: Desktop extension allowlist
            url: https://support.claude.com/en/articles/12592343-enabling-and-using-the-desktop-extension-allowlist
          - title: Org-wide MCP connector authorization
            url: https://support.claude.com/en/articles/15537633-authorize-mcp-connectors-for-your-entire-organization
          - title: Use connectors (admin controls)
            url: https://support.claude.com/en/articles/11176164-use-connectors-to-extend-claude-s-capabilities
      - title: Identity, audit and compliance for the organisation
        body:
          - SSO via WorkOS (Okta, Entra ID, Google, OneLogin, JumpCloud, Duo SAML) with "Require
            SSO for Claude", JIT or SCIM provisioning with group-to-role mapping and automatic
            removal; Enterprise custom roles and groups; IP allowlisting (CIDR ranges enabled by
            Anthropic support); tenant restrictions via the proxy-injected
            anthropic-allowed-org-ids header.
          - Audit logs (Enterprise, 180 days, emailed download link) from Organization settings >
            Data and privacy; the Compliance API for the Activity Feed, chats, files, projects, directory,
            effective settings and Cowork / Claude Code / M365 session transcripts; custom data
            retention from 30 days; Inference hooks (beta) for inline allow/deny of every governed
            prompt across claude.ai, Cowork and Claude Code.
        links:
          - title: Set up single sign-on
            url: https://support.claude.com/en/articles/13132885-set-up-single-sign-on-sso
          - title: Set up JIT or SCIM provisioning
            url: https://support.claude.com/en/articles/13133195-set-up-jit-or-scim-provisioning
          - title: Compliance API
            url: https://platform.claude.com/docs/en/manage-claude/compliance-api
    riskNotes:
      - risk: riskSensitiveDataDisclosure
        note: >-
          Chat and project retention is indefinite until an Owner sets a period; uploads,
          connector results and artifacts persist with the chat. Custom retention and the
          Compliance API delete endpoints are the levers.
      - risk: riskToolSourceProvenance
        note: >-
          Custom remote MCP connectors and desktop extensions are user-addable by default; the
          extension allowlist ships disabled and Anthropic reviews Directory connectors against
          listing criteria without auditing them.
      - risk: riskAgenticDelegationConfusedDeputy
        note: >-
          Connectors act with the user's granted scopes; org-wide tool restrictions (read vs
          write) and managed authorisation scoping keep Claude from sending mail or writing
          tickets when only reading is intended.
      - risk: riskShadowAndUnknownAgents
        note: >-
          Personal accounts on the corporate network are blocked only by tenant restrictions or IP
          allowlisting; domain capture and SSO enforcement pull verified-domain users into the
          organisation.
    controls:
      - mitigation: D3-MFA
        coverage: unknown
        mechanism: SSO (SAML/OIDC) with Require SSO for Claude, domain verification and capture, JIT or SCIM
          provisioning with group-to-role mapping, custom roles and groups (Enterprise)
        steps:
          - &a1
            title: Enforce SSO
            body:
              - Verify domains at claude.ai/admin-settings/organization, configure the IdP through WorkOS, then toggle
                "Require SSO for Claude" in Authentication settings.
            url: https://support.claude.com/en/articles/13132885-set-up-single-sign-on-sso
          - &a2
            title: Provision and deprovision from the directory
            body:
              - Enable SCIM with group-to-role mapping (Enterprise / Console); users removed from all mapped groups
                are removed at the next sync.
            url: https://support.claude.com/en/articles/13133195-set-up-jit-or-scim-provisioning
          - &a3
            title: Gate features by role
            body:
              - Create custom roles and groups at Organization settings > Roles and Groups to gate Claude Code,
                Cowork, Chrome, connectors, Claude Tag, models and admin scopes.
            url: https://support.claude.com/en/articles/13930458-set-up-role-based-permissions-on-enterprise-plans
        evidence: []
        note: >-
          Capability scope changed: reassess this function. Retained configuration and evidence describe the
          former capability, not verified coverage of every replacement.


          [capabilityIga]
        migration:
          from:
            - capabilityIga
          reviewRequired: true
          original:
            - capability: capabilityIga
              coverage: native
              mechanism: SSO (SAML/OIDC) with Require SSO for Claude, domain verification and capture, JIT or SCIM
                provisioning with group-to-role mapping, custom roles and groups (Enterprise)
              steps: &a4
                - *a1
                - *a2
                - *a3
              verified: 2026-09-10
      - mitigation: D3-APA
        coverage: unknown
        mechanism: SSO (SAML/OIDC) with Require SSO for Claude, domain verification and capture, JIT or SCIM
          provisioning with group-to-role mapping, custom roles and groups (Enterprise); Connectors retrieve
          as the authenticated user (OAuth) or as the scoped managed authorisation; verified-domain
          restriction stops org services connecting to outside accounts
        steps:
          - *a1
          - *a2
          - *a3
          - &a5
            title: Keep retrieval per user
            body:
              - Prefer user OAuth or role-scoped managed authorisation over shared service accounts and turn on the
                verified-domain connector restriction on Enterprise.
            url: https://support.claude.com/en/articles/11176164-use-connectors-to-extend-claude-s-capabilities
        evidence: []
        note: >-
          Capability scope changed: reassess this function. Retained configuration and evidence describe the
          former capability, not verified coverage of every replacement.


          [capabilityIga]


          [capabilityDataAccessGovernance] Delegated when the connector runs as the user (the source system
          enforces the user's permissions, oversharing included); service-owned when enterprise-managed
          authorisation is used, in which case the managed scope, not the user's entitlements, bounds
          retrieval for everyone.
        migration:
          from:
            - capabilityIga
            - capabilityDataAccessGovernance
          reviewRequired: true
          original:
            - capability: capabilityIga
              coverage: native
              mechanism: SSO (SAML/OIDC) with Require SSO for Claude, domain verification and capture, JIT or SCIM
                provisioning with group-to-role mapping, custom roles and groups (Enterprise)
              steps: *a4
              verified: 2026-09-10
            - capability: capabilityDataAccessGovernance
              coverage: partial
              mechanism: Connectors retrieve as the authenticated user (OAuth) or as the scoped managed authorisation;
                verified-domain restriction stops org services connecting to outside accounts
              steps: &a26
                - *a5
              note: Delegated when the connector runs as the user (the source system enforces the user's permissions,
                oversharing included); service-owned when enterprise-managed authorisation is used, in which
                case the managed scope, not the user's entitlements, bounds retrieval for everyone.
              verified: 2026-09-10
      - mitigation: D3-OTF
        coverage: unknown
        mechanism: IP allowlisting (Enterprise, CIDR ranges enabled by Anthropic support) and tenant restrictions
          (anthropic-allowed-org-ids header injected by a TLS-inspecting proxy); Documented host allowlist for
          Claude Desktop and claude.ai; egressProxyUrl / PAC keys on 3P; connector traffic through
          mcp-proxy.anthropic.com; custom connectors reachable only via allowlisted Anthropic IPs
        steps:
          - &a6
            title: Restrict where Claude can be used from
            body:
              - Compile CIDR ranges and have Anthropic enable IP allowlisting; every authenticated request outside the
                ranges is denied (Anthropic-hosted cloud sessions then need an exemption).
            url: https://support.claude.com/en/articles/13200993-restrict-access-to-claude-with-ip-allowlisting
          - &a7
            title: Block other tenants at the proxy
            body:
              - Inject anthropic-allowed-org-ids with your org UUIDs for claude.ai, api.anthropic.com, claude.com and
                anthropic.com (up to 500 UUIDs across numbered headers).
            url: https://support.claude.com/en/articles/13198485-enforce-network-level-access-control-with-tenant-restrictions
          - &a8
            title: Allowlist the documented hosts
            body:
              - Permit anthropic.com, claude.ai, claude.com, platform.claude.com, downloads.claude.ai,
                *.claudeusercontent.com and *.claudemcpcontent.com (plus optional CDN hosts for artifacts) and
                route through egressProxyUrl on managed devices.
            url: https://code.claude.com/docs/en/desktop
        evidence: []
        note: >-
          Capability scope changed: reassess this function. Retained configuration and evidence describe the
          former capability, not verified coverage of every replacement.


          [capabilitySse] IP allowlisting is enforced by Anthropic; the tenant-restriction header is enforced
          by a TLS-inspecting proxy the organisation operates. Neither is an SSE service: inline inspection of
          prompts and uploads is the customer's SSE, and its completeness depends on which paths it sees.


          [capabilityEgressControl] The product documents its destinations and offers proxy keys; it enforces
          no destination policy of its own. Connector traffic egresses from Anthropic's MCP proxy, outside the
          organisation's network.
        migration:
          from:
            - capabilitySse
            - capabilityEgressControl
          reviewRequired: true
          original:
            - capability: capabilitySse
              coverage: partial
              mechanism: IP allowlisting (Enterprise, CIDR ranges enabled by Anthropic support) and tenant restrictions
                (anthropic-allowed-org-ids header injected by a TLS-inspecting proxy)
              steps:
                - *a6
                - *a7
              note: "IP allowlisting is enforced by Anthropic; the tenant-restriction header is enforced by a TLS-inspecting
                proxy the organisation operates. Neither is an SSE service: inline inspection of prompts and
                uploads is the customer's SSE, and its completeness depends on which paths it sees."
              verified: 2026-09-10
            - capability: capabilityEgressControl
              coverage: partial
              mechanism: Documented host allowlist for Claude Desktop and claude.ai; egressProxyUrl / PAC keys on 3P;
                connector traffic through mcp-proxy.anthropic.com; custom connectors reachable only via
                allowlisted Anthropic IPs
              steps: &a12
                - *a8
              note: The product documents its destinations and offers proxy keys; it enforces no destination policy of its
                own. Connector traffic egresses from Anthropic's MCP proxy, outside the organisation's
                network.
              verified: 2026-09-10
      - mitigation: AML.M0020
        coverage: unknown
        mechanism: Inference hooks (Enterprise, beta) route every governed prompt to your AI security server for
          allow/deny; connector tool restrictions limit outbound actions; no built-in content classifier;
          Anthropic model-side safeguards and web-fetch isolation; connector tool restrictions; Inference
          hooks for inline policy
        steps:
          - &a9
            title: Enable Inference hooks
            body:
              - Configure the AI security server endpoint and signing secret, run shadow mode, then enforce; denied
                requests never reach the model and are recorded in the Activity Feed. Not available on Bedrock
                or Google Cloud.
            url: https://platform.claude.com/docs/en/manage-claude/inference-hooks
          - &a10
            title: Limit what an injected instruction can do
            body:
              - Restrict connectors to read-only tools and enable only vetted connectors; use Inference hooks to deny
                prompts carrying policy-violating content.
            url: https://support.claude.com/en/articles/11176164-use-connectors-to-extend-claude-s-capabilities
        evidence: []
        note: >-
          Capability scope changed: reassess this function. Retained configuration and evidence describe the
          former capability, not verified coverage of every replacement.


          [capabilityAiDlp] Inference hooks route governed prompts to the organisation's scanner; the product
          itself classifies nothing, and uploads, connector results and outputs are outside the hook unless
          the hook contract says otherwise.


          [capabilityPromptInjectionDefense] No admin-configurable injection classifier is exposed for chat.
        migration:
          from:
            - capabilityAiDlp
            - capabilityPromptInjectionDefense
          reviewRequired: true
          original:
            - capability: capabilityAiDlp
              coverage: partial
              mechanism: Inference hooks (Enterprise, beta) route every governed prompt to your AI security server for
                allow/deny; connector tool restrictions limit outbound actions; no built-in content classifier
              steps:
                - *a9
              note: Inference hooks route governed prompts to the organisation's scanner; the product itself classifies
                nothing, and uploads, connector results and outputs are outside the hook unless the hook
                contract says otherwise.
              verified: 2026-09-10
            - capability: capabilityPromptInjectionDefense
              coverage: partial
              mechanism: Anthropic model-side safeguards and web-fetch isolation; connector tool restrictions; Inference
                hooks for inline policy
              steps:
                - *a10
              note: No admin-configurable injection classifier is exposed for chat.
              verified: 2026-09-10
      - mitigation: D3-WSAA
        coverage: external
        verified: "2026-09-10"
        mechanism: claude.ai is a browser SaaS; paste and upload inspection is the organisation's browser-security
          tooling
        steps:
          - &a11
            title: Instrument the browser session
            body:
              - Apply your browser-security policy to claude.ai and *.claudeusercontent.com and use the
                tenant-restriction header to keep sessions in the corporate tenant.
            url: https://support.claude.com/en/articles/13198485-enforce-network-level-access-control-with-tenant-restrictions
        evidence: []
        note: >-
          Anthropic offers no in-page inspection; pair the tenant-restriction header with browser D&R or SSE
          inline controls.
        migration:
          from:
            - capabilityBrowserDetectionResponse
          reviewRequired: false
          original:
            - capability: capabilityBrowserDetectionResponse
              coverage: external
              mechanism: claude.ai is a browser SaaS; paste and upload inspection is the organisation's browser-security
                tooling
              note: Anthropic offers no in-page inspection; pair the tenant-restriction header with browser D&R or SSE
                inline controls.
              steps:
                - *a11
              verified: 2026-09-10
      - mitigation: D3-NI
        coverage: unknown
        mechanism: Documented host allowlist for Claude Desktop and claude.ai; egressProxyUrl / PAC keys on 3P;
          connector traffic through mcp-proxy.anthropic.com; custom connectors reachable only via allowlisted
          Anthropic IPs
        steps:
          - *a8
        evidence: []
        note: >-
          Capability scope changed: reassess this function. Retained configuration and evidence describe the
          former capability, not verified coverage of every replacement.


          [capabilityEgressControl] The product documents its destinations and offers proxy keys; it enforces
          no destination policy of its own. Connector traffic egresses from Anthropic's MCP proxy, outside the
          organisation's network.
        migration:
          from:
            - capabilityEgressControl
          reviewRequired: true
          original:
            - capability: capabilityEgressControl
              coverage: partial
              mechanism: Documented host allowlist for Claude Desktop and claude.ai; egressProxyUrl / PAC keys on 3P;
                connector traffic through mcp-proxy.anthropic.com; custom connectors reachable only via
                allowlisted Anthropic IPs
              steps: *a12
              note: The product documents its destinations and offers proxy keys; it enforces no destination policy of its
                own. Connector traffic egresses from Anthropic's MCP proxy, outside the organisation's
                network.
              verified: 2026-09-10
      - mitigation: D3-ORA
        coverage: unknown
        mechanism: Compliance API directory and settings endpoints, Enterprise Analytics API and usage exports feed
          the organisation's governance platform; Trust Center artifacts (SOC 2, ISO 27001, HIPAA
          implementation guide, architecture overviews) feed the buyer's TPRM platform
        steps:
          - &a13
            title: Mirror organisation state
            body:
              - Pull organizations, users, roles, groups and effective settings from the Compliance API and per-user
                engagement from the Enterprise Analytics API.
            url: https://platform.claude.com/docs/en/manage-claude/compliance-api
          - &a14
            title: Collect artifacts
            body:
              - Request the Trust Center resources referenced across the docs, including the Cowork desktop security
                architecture overview and the M365 architecture diagrams.
            url: https://trust.anthropic.com
        evidence: []
        note: >-
          Capability scope changed: reassess this function. Retained configuration and evidence describe the
          former capability, not verified coverage of every replacement.


          [capabilityAiGovernancePlatform]


          [capabilityAiTprm]
        migration:
          from:
            - capabilityAiGovernancePlatform
            - capabilityAiTprm
          reviewRequired: true
          original:
            - capability: capabilityAiGovernancePlatform
              coverage: external
              mechanism: Compliance API directory and settings endpoints, Enterprise Analytics API and usage exports feed
                the organisation's governance platform
              steps:
                - *a13
              verified: 2026-09-10
            - capability: capabilityAiTprm
              coverage: external
              mechanism: Trust Center artifacts (SOC 2, ISO 27001, HIPAA implementation guide, architecture overviews) feed
                the buyer's TPRM platform
              steps:
                - *a14
              verified: 2026-09-10
      - mitigation: AML.M0029
        coverage: partial
        verified: "2026-09-10"
        mechanism: Connector tool calls prompt per tool unless the user chooses always allow; Owners can set connector
          tools to ask or blocked org-wide; no approval workflow for chat outputs
        steps:
          - &a15
            title: Force approval on connector actions
            body:
              - At Organization settings > Connectors set tools that write (send message, create issue) to ask or remove
                them; these settings are enforced in Claude Code and Cowork sessions too.
            url: https://support.claude.com/en/articles/11176164-use-connectors-to-extend-claude-s-capabilities
        evidence: []
        migration:
          from:
            - capabilityHitlControls
          reviewRequired: false
          original:
            - capability: capabilityHitlControls
              coverage: partial
              mechanism: Connector tool calls prompt per tool unless the user chooses always allow; Owners can set connector
                tools to ask or blocked org-wide; no approval workflow for chat outputs
              steps:
                - *a15
              verified: 2026-09-10
      - mitigation: D3-AA
        coverage: unknown
        mechanism: Compliance Access Keys and Admin API keys with scopes; connector service identities via
          enterprise-managed authorisation; no per-agent identity for chat
        steps:
          - &a16
            title: Scope machine credentials
            body:
              - Create a Compliance Access Key with only the scopes needed (read:compliance_activities,
                read:compliance_user_data) rather than an Owner's session.
            url: https://platform.claude.com/docs/en/manage-claude/compliance-api
        evidence: []
        note: >-
          Capability scope changed: reassess this function. Retained configuration and evidence describe the
          former capability, not verified coverage of every replacement.


          [capabilityNhiManagement] The scoped API keys identify automation reading the organisation, not an
          agent acting for users; connector service identities are the vendor's; chat actions carry the user's
          identity.
        migration:
          from:
            - capabilityNhiManagement
          reviewRequired: true
          original:
            - capability: capabilityNhiManagement
              coverage: partial
              mechanism: Compliance Access Keys and Admin API keys with scopes; connector service identities via
                enterprise-managed authorisation; no per-agent identity for chat
              steps: &a17
                - *a16
              note: The scoped API keys identify automation reading the organisation, not an agent acting for users;
                connector service identities are the vendor's; chat actions carry the user's identity.
              verified: 2026-09-10
      - mitigation: D3-CR
        coverage: unknown
        mechanism: Compliance Access Keys and Admin API keys with scopes; connector service identities via
          enterprise-managed authorisation; no per-agent identity for chat; SCIM deprovisioning, member
          removal, connector disable (revokes for all), extension allowlist force-delete, device keys
          (isClaudeCodeForDesktopEnabled, secureVmFeaturesEnabled, isDesktopExtensionEnabled), Compliance API
          delete
        steps:
          - *a16
          - &a18
            title: Cut access and remove data
            body:
              - Remove the member or SCIM group, disable the connector at Organization settings > Connectors, and delete
                chats, files or projects through the Compliance API.
            url: https://platform.claude.com/docs/en/manage-claude/compliance-api
        evidence: []
        note: >-
          Capability scope changed: reassess this function. Retained configuration and evidence describe the
          former capability, not verified coverage of every replacement.


          [capabilityNhiManagement] The scoped API keys identify automation reading the organisation, not an
          agent acting for users; connector service identities are the vendor's; chat actions carry the user's
          identity.


          [capabilityKillSwitch] Seat removal and connector disable apply at the next request; a connector
          call in flight completes. The docs state no propagation delay, so measure it.
        migration:
          from:
            - capabilityNhiManagement
            - capabilityKillSwitch
          reviewRequired: true
          original:
            - capability: capabilityNhiManagement
              coverage: partial
              mechanism: Compliance Access Keys and Admin API keys with scopes; connector service identities via
                enterprise-managed authorisation; no per-agent identity for chat
              steps: *a17
              note: The scoped API keys identify automation reading the organisation, not an agent acting for users;
                connector service identities are the vendor's; chat actions carry the user's identity.
              verified: 2026-09-10
            - capability: capabilityKillSwitch
              coverage: native
              mechanism: SCIM deprovisioning, member removal, connector disable (revokes for all), extension allowlist
                force-delete, device keys (isClaudeCodeForDesktopEnabled, secureVmFeaturesEnabled,
                isDesktopExtensionEnabled), Compliance API delete
              steps: &a31
                - *a18
              note: Seat removal and connector disable apply at the next request; a connector call in flight completes. The
                docs state no propagation delay, so measure it.
              verified: 2026-09-10
      - mitigation: D3-CI
        coverage: external
        verified: "2026-09-10"
        mechanism: Compliance API effective-settings and Activity Feed endpoints, audit log export, and admin console
          readback let posture tooling verify training opt-out, retention, connectors and roles
        steps:
          - &a19
            title: Evidence configuration continuously
            body:
              - Poll each organisation's effective settings and the Activity Feed with a Compliance Access Key; use the
                audit log export (180 days) for point-in-time evidence.
            url: https://platform.claude.com/docs/en/manage-claude/compliance-api
        evidence: []
        note: >-
          The Compliance API effective-settings and Activity Feed endpoints are the readback an SSPM product
          polls; nothing in the product compares settings to a baseline, detects drift or remediates. The same
          rule applies to every vendor with a readback API.
        migration:
          from:
            - capabilityVendorAssurance
          reviewRequired: false
          original:
            - capability: capabilityVendorAssurance
              coverage: external
              mechanism: Compliance API effective-settings and Activity Feed endpoints, audit log export, and admin console
                readback let posture tooling verify training opt-out, retention, connectors and roles
              steps:
                - *a19
              note: The Compliance API effective-settings and Activity Feed endpoints are the readback an SSPM product
                polls; nothing in the product compares settings to a baseline, detects drift or remediates.
                The same rule applies to every vendor with a readback API.
              verified: 2026-09-10
      - mitigation: D3-DI
        coverage: partial
        verified: "2026-09-10"
        mechanism: Custom data retention (30 days to indefinite) for chats and projects, Compliance API retrieve and
          delete for chats, files and projects, organisation data export
        steps:
          - &a20
            title: Set retention and delete on demand
            body:
              - Configure retention at Organization settings > Data and privacy (Owner) and use the Compliance API
                content endpoints to retrieve or delete chats, files and projects.
            url: https://support.claude.com/en/articles/10440198-configure-custom-data-retention-controls-for-enterprise-plans
        evidence: []
        note: >-
          Retention, deletion and export are data lifecycle controls. They do not discover or classify
          sensitive content in chats, files or projects, nor assess exposure; a DSPM product consumes the
          Compliance API content endpoints to do that.
        migration:
          from:
            - capabilityDspm
          reviewRequired: false
          original:
            - capability: capabilityDspm
              coverage: partial
              mechanism: Custom data retention (30 days to indefinite) for chats and projects, Compliance API retrieve and
                delete for chats, files and projects, organisation data export
              steps:
                - *a20
              note: Retention, deletion and export are data lifecycle controls. They do not discover or classify sensitive
                content in chats, files or projects, nor assess exposure; a DSPM product consumes the
                Compliance API content endpoints to do that.
              verified: 2026-09-10
      - mitigation: AML.M0012
        coverage: native
        verified: "2026-09-10"
        mechanism: Customer-managed encryption keys (Enterprise; AWS KMS, Azure Key Vault, Google Cloud KMS) and
          US-only inference; TLS in transit
        steps:
          - &a21
            title: Bring your own key
            body:
              - Provision a key in your cloud KMS and enable CMEK on the Enterprise plan; note the audit-log export is
                unavailable under CMEK (use the Compliance API) and local session transcripts are encrypted
                under the key.
            url: https://support.claude.com/en/articles/9797531-what-is-the-enterprise-plan
        evidence: []
        note: >-
          Assets and key owners: chats, files and projects at Anthropic (customer-managed keys on Enterprise;
          Anthropic keys otherwise); traffic (TLS); the desktop app's local cache (host disk encryption, the
          organisation's MDM). CMEK does not cover the local cache.
        migration:
          from:
            - capabilityEncryptionKeyManagement
          reviewRequired: false
          original:
            - capability: capabilityEncryptionKeyManagement
              coverage: native
              mechanism: Customer-managed encryption keys (Enterprise; AWS KMS, Azure Key Vault, Google Cloud KMS) and
                US-only inference; TLS in transit
              steps:
                - *a21
              note: "Assets and key owners: chats, files and projects at Anthropic (customer-managed keys on Enterprise;
                Anthropic keys otherwise); traffic (TLS); the desktop app's local cache (host disk encryption,
                the organisation's MDM). CMEK does not cover the local cache."
              verified: 2026-09-10
      - mitigation: D3-CH
        coverage: unknown
        mechanism: Connector OAuth tokens held by the platform; enterprise-managed authorisation replaces per-user
          grants; API and compliance keys are scoped and revocable in the console
        steps:
          - &a22
            title: Centralise connector credentials
            body:
              - Use enterprise-managed authorisation so connector access is inherited from IdP roles on first login
                instead of users pasting tokens.
            url: https://support.claude.com/en/articles/15537633-authorize-mcp-connectors-for-your-entire-organization
        evidence: []
        note: >-
          Capability scope changed: reassess this function. Retained configuration and evidence describe the
          former capability, not verified coverage of every replacement.


          [capabilitySecretsManagement] Connector tokens are held by the platform and never shown to the
          model; the API and compliance keys are long-lived until rotated in the console. Enterprise-managed
          authorisation replaces per-user grants — it does not make the underlying credential ephemeral.
        migration:
          from:
            - capabilitySecretsManagement
          reviewRequired: true
          original:
            - capability: capabilitySecretsManagement
              coverage: partial
              mechanism: Connector OAuth tokens held by the platform; enterprise-managed authorisation replaces per-user
                grants; API and compliance keys are scoped and revocable in the console
              steps: &a23
                - *a22
              note: Connector tokens are held by the platform and never shown to the model; the API and compliance keys are
                long-lived until rotated in the console. Enterprise-managed authorisation replaces per-user
                grants — it does not make the underlying credential ephemeral.
              verified: 2026-09-10
      - mitigation: D3-CRO
        coverage: unknown
        mechanism: Connector OAuth tokens held by the platform; enterprise-managed authorisation replaces per-user
          grants; API and compliance keys are scoped and revocable in the console
        steps:
          - *a22
        evidence: []
        note: >-
          Capability scope changed: reassess this function. Retained configuration and evidence describe the
          former capability, not verified coverage of every replacement.


          [capabilitySecretsManagement] Connector tokens are held by the platform and never shown to the
          model; the API and compliance keys are long-lived until rotated in the console. Enterprise-managed
          authorisation replaces per-user grants — it does not make the underlying credential ephemeral.
        migration:
          from:
            - capabilitySecretsManagement
          reviewRequired: true
          original:
            - capability: capabilitySecretsManagement
              coverage: partial
              mechanism: Connector OAuth tokens held by the platform; enterprise-managed authorisation replaces per-user
                grants; API and compliance keys are scoped and revocable in the console
              steps: *a23
              note: Connector tokens are held by the platform and never shown to the model; the API and compliance keys are
                long-lived until rotated in the console. Enterprise-managed authorisation replaces per-user
                grants — it does not make the underlying credential ephemeral.
              verified: 2026-09-10
      - mitigation: AML.M0028
        coverage: native
        verified: "2026-09-10"
        mechanism: Connector enablement per organisation, per-tool read/write restrictions, role-based connector
          access, desktop extension allowlist, isLocalDevMcpEnabled, isDesktopExtensionEnabled
        steps:
          - &a24
            title: Enable and scope connectors
            body:
              - Enable connectors at Organization settings > Connectors > Browse connectors and restrict each connector's
                tools (for example search but not send) for the whole organisation.
            url: https://support.claude.com/en/articles/11176164-use-connectors-to-extend-claude-s-capabilities
          - &a25
            title: Lock down device-side tools
            body:
              - Turn on the desktop extension allowlist and set isLocalDevMcpEnabled or isDesktopExtensionEnabled
                false in device policy where local tools are not wanted.
            url: https://support.claude.com/en/articles/12592343-enabling-and-using-the-desktop-extension-allowlist
        evidence: []
        note: >-
          Connector enablement and per-tool read/write restrictions hide or limit connector tools org-wide;
          there is no argument-level scoping (which folder, which channel) beyond what the connector's own
          authorisation grants.
        migration:
          from:
            - capabilityToolPermissionScoping
          reviewRequired: false
          original:
            - capability: capabilityToolPermissionScoping
              coverage: native
              mechanism: Connector enablement per organisation, per-tool read/write restrictions, role-based connector
                access, desktop extension allowlist, isLocalDevMcpEnabled, isDesktopExtensionEnabled
              steps:
                - *a24
                - *a25
              note: Connector enablement and per-tool read/write restrictions hide or limit connector tools org-wide; there
                is no argument-level scoping (which folder, which channel) beyond what the connector's own
                authorisation grants.
              verified: 2026-09-10
      - mitigation: D3-AMED
        coverage: unknown
        mechanism: Connectors retrieve as the authenticated user (OAuth) or as the scoped managed authorisation;
          verified-domain restriction stops org services connecting to outside accounts
        steps:
          - *a5
        evidence: []
        note: >-
          Capability scope changed: reassess this function. Retained configuration and evidence describe the
          former capability, not verified coverage of every replacement.


          [capabilityDataAccessGovernance] Delegated when the connector runs as the user (the source system
          enforces the user's permissions, oversharing included); service-owned when enterprise-managed
          authorisation is used, in which case the managed scope, not the user's entitlements, bounds
          retrieval for everyone.
        migration:
          from:
            - capabilityDataAccessGovernance
          reviewRequired: true
          original:
            - capability: capabilityDataAccessGovernance
              coverage: partial
              mechanism: Connectors retrieve as the authenticated user (OAuth) or as the scoped managed authorisation;
                verified-domain restriction stops org services connecting to outside accounts
              steps: *a26
              note: Delegated when the connector runs as the user (the source system enforces the user's permissions,
                oversharing included); service-owned when enterprise-managed authorisation is used, in which
                case the managed scope, not the user's entitlements, bounds retrieval for everyone.
              verified: 2026-09-10
      - mitigation: D3-AI
        coverage: unknown
        mechanism: Organisation connector catalogue, extension allowlist and role assignments in the console;
          Compliance API session listings for Cowork and Claude Code; Domain capture pulls verified-domain
          accounts into the organisation; tenant restrictions and IP allowlisting block personal accounts on
          corporate networks
        steps:
          - &a27
            title: Treat the connector catalogue as the registry
            body:
              - Only Owner-enabled connectors and allowlisted extensions are available to members; review them under
                Organization settings > Connectors.
            url: https://support.claude.com/en/articles/12592343-enabling-and-using-the-desktop-extension-allowlist
          - &a28
            title: Capture and block
            body:
              - Verify domains for account claiming and deploy the tenant-restriction header so personal claude.ai
                use from managed networks is refused.
            url: https://support.claude.com/en/articles/13198485-enforce-network-level-access-control-with-tenant-restrictions
        evidence: []
        note: >-
          Capability scope changed: reassess this function. Retained configuration and evidence describe the
          former capability, not verified coverage of every replacement.


          [capabilityAgentRegistry] The connector catalogue and extension allowlist are approved
          configuration; session listings are use. There is no owner or approval record per connector and no
          reconciliation — the organisation's inventory holds those.


          [capabilityShadowAiDiscovery] Domain capture is a genuine discovery signal: it finds personal
          accounts on verified domains and pulls them into the organisation. Its scope is accounts on your
          domains — personal accounts on other addresses and unmanaged devices are found by proxy or endpoint
          inventory.
        migration:
          from:
            - capabilityAgentRegistry
            - capabilityShadowAiDiscovery
          reviewRequired: true
          original:
            - capability: capabilityAgentRegistry
              coverage: partial
              mechanism: Organisation connector catalogue, extension allowlist and role assignments in the console;
                Compliance API session listings for Cowork and Claude Code
              steps:
                - *a27
              note: The connector catalogue and extension allowlist are approved configuration; session listings are use.
                There is no owner or approval record per connector and no reconciliation — the organisation's
                inventory holds those.
              verified: 2026-09-10
            - capability: capabilityShadowAiDiscovery
              coverage: partial
              mechanism: Domain capture pulls verified-domain accounts into the organisation; tenant restrictions and IP
                allowlisting block personal accounts on corporate networks
              steps:
                - *a28
              note: "Domain capture is a genuine discovery signal: it finds personal accounts on verified domains and pulls
                them into the organisation. Its scope is accounts on your domains — personal accounts on other
                addresses and unmanaged devices are found by proxy or endpoint inventory."
              verified: 2026-09-10
      - mitigation: AML.M0024
        coverage: unknown
        mechanism: Enterprise audit logs (30+ event types, 180-day export) and the Compliance API Activity Feed with
          actor, IP, user agent and device; session transcripts for Cowork, Claude Code, M365 and Science
        steps:
          - &a29
            title: Export or stream audit events
            body:
              - Owners export logs from Organization settings > Data and privacy, or query GET /v1/compliance/activities
                continuously into the SIEM.
            url: https://support.claude.com/en/articles/9970975-access-audit-logs
        evidence: []
        note: >-
          Capability scope changed: reassess this function. Retained configuration and evidence describe the
          former capability, not verified coverage of every replacement.


          [capabilityAuditLogging] Vendor-held audit log with a 180-day export window and the Activity Feed;
          durable retention and tamper evidence beyond that window are the organisation's archive. Events are
          attributed, not signed.
        migration:
          from:
            - capabilityAuditLogging
          reviewRequired: true
          original:
            - capability: capabilityAuditLogging
              coverage: native
              mechanism: Enterprise audit logs (30+ event types, 180-day export) and the Compliance API Activity Feed with
                actor, IP, user agent and device; session transcripts for Cowork, Claude Code, M365 and
                Science
              steps: &a30
                - *a29
              note: Vendor-held audit log with a 180-day export window and the Activity Feed; durable retention and tamper
                evidence beyond that window are the organisation's archive. Events are attributed, not signed.
              verified: 2026-09-10
      - mitigation: D3-SFA
        coverage: unknown
        mechanism: Enterprise audit logs (30+ event types, 180-day export) and the Compliance API Activity Feed with
          actor, IP, user agent and device; session transcripts for Cowork, Claude Code, M365 and Science
        steps:
          - *a29
        evidence: []
        note: >-
          Capability scope changed: reassess this function. Retained configuration and evidence describe the
          former capability, not verified coverage of every replacement.


          [capabilityAuditLogging] Vendor-held audit log with a 180-day export window and the Activity Feed;
          durable retention and tamper evidence beyond that window are the organisation's archive. Events are
          attributed, not signed.
        migration:
          from:
            - capabilityAuditLogging
          reviewRequired: true
          original:
            - capability: capabilityAuditLogging
              coverage: native
              mechanism: Enterprise audit logs (30+ event types, 180-day export) and the Compliance API Activity Feed with
                actor, IP, user agent and device; session transcripts for Cowork, Claude Code, M365 and
                Science
              steps: *a30
              note: Vendor-held audit log with a 180-day export window and the Activity Feed; durable retention and tamper
                evidence beyond that window are the organisation's archive. Events are attributed, not signed.
              verified: 2026-09-10
      - mitigation: D3-PT
        coverage: unknown
        mechanism: SCIM deprovisioning, member removal, connector disable (revokes for all), extension allowlist
          force-delete, device keys (isClaudeCodeForDesktopEnabled, secureVmFeaturesEnabled,
          isDesktopExtensionEnabled), Compliance API delete
        steps:
          - *a18
        evidence: []
        note: >-
          Capability scope changed: reassess this function. Retained configuration and evidence describe the
          former capability, not verified coverage of every replacement.


          [capabilityKillSwitch] Seat removal and connector disable apply at the next request; a connector
          call in flight completes. The docs state no propagation delay, so measure it.
        migration:
          from:
            - capabilityKillSwitch
          reviewRequired: true
          original:
            - capability: capabilityKillSwitch
              coverage: native
              mechanism: SCIM deprovisioning, member removal, connector disable (revokes for all), extension allowlist
                force-delete, device keys (isClaudeCodeForDesktopEnabled, secureVmFeaturesEnabled,
                isDesktopExtensionEnabled), Compliance API delete
              steps: *a31
              note: Seat removal and connector disable apply at the next request; a connector call in flight completes. The
                docs state no propagation delay, so measure it.
              verified: 2026-09-10
    sources:
      - title: What is the Enterprise plan
        url: https://support.claude.com/en/articles/9797531-what-is-the-enterprise-plan
      - title: Claude Enterprise administrator guide
        url: https://academy.claude.com/tutorials/claude-enterprise-administrator-guide
      - title: Deploy Claude Desktop for macOS
        url: https://support.claude.com/en/articles/12611117-deploy-claude-desktop-for-macos
      - title: Deploy Claude Desktop for Windows
        url: https://support.claude.com/en/articles/12622703-deploy-claude-desktop-for-windows
      - title: Enterprise configuration for Claude Desktop
        url: https://support.claude.com/en/articles/12622667-enterprise-configuration-for-claude-desktop
      - title: Claude Desktop on 3P overview
        url: https://claude.com/docs/third-party/claude-desktop/overview
      - title: Set up role-based permissions on Enterprise plans
        url: https://support.claude.com/en/articles/13930458-set-up-role-based-permissions-on-enterprise-plans
      - title: Access audit logs
        url: https://support.claude.com/en/articles/9970975-access-audit-logs
      - title: Configure custom data retention controls
        url: https://support.claude.com/en/articles/10440198-configure-custom-data-retention-controls-for-enterprise-plans
      - title: Restrict access with IP allowlisting
        url: https://support.claude.com/en/articles/13200993-restrict-access-to-claude-with-ip-allowlisting
      - title: Inference hooks
        url: https://platform.claude.com/docs/en/manage-claude/inference-hooks
      - title: Retrieve session transcripts (Compliance API)
        url: https://platform.claude.com/docs/en/manage-claude/compliance-sessions
